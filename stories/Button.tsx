import React from 'react'
import './button.css'
import { CommonButton } from 'component'

/**
 * Primary UI component for user interaction
 */
export const Buttons = () => {
  return <CommonButton data={[]}>sdfsdf</CommonButton>
}
